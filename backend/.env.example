# Anthropic API key for AI policy advisor
ANTHROPIC_API_KEY=sk-ant-xxx

# Optional: Email notification (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@email.com
SMTP_PASS=your_app_password

# Crawler settings
CRAWL_INTERVAL_HOURS=6
